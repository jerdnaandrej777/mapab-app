name: travel_planner
description: Eine europäische Reiseplanungs-App mit AI-Integration
publish_to: 'none'
version: 1.10.29+212

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Navigation
  go_router: ^13.0.0

  # Karten
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  maplibre_gl: ^0.25.0        # 3D Navigation mit Pitch/Tilt

  # HTTP & Networking
  dio: ^5.4.0
  dio_cache_interceptor: ^3.5.0

  # Lokale Datenbank
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Sichere Credential-Speicherung (Android Keystore / iOS Keychain)
  flutter_secure_storage: ^9.2.4

  # Cloud Backend (Auth & Sync)
  supabase_flutter: ^2.8.0

  # GPS & Location
  geolocator: ^11.1.0
  geocoding: ^3.0.0

  # OpenAI Integration
  dart_openai: ^5.1.0

  # Immutable Models
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

  # UI Helpers
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0

  # Charts (Hoehenprofil)
  fl_chart: ^0.69.0

  # Utils
  intl: ^0.20.2
  uuid: ^4.3.3
  url_launcher: ^6.2.2
  share_plus: ^7.2.1

  # Voice & TTS
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2

  # Photo Viewer
  photo_view: ^0.14.0

  # Image Picker (Reisetagebuch)
  image_picker: ^1.0.7
  path_provider: ^2.1.1

  # QR Code (für Sharing + Scannen)
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1

  # Crash Reporting
  sentry_flutter: ^8.12.0

  # Background Navigation (Foreground Service)
  flutter_foreground_task: ^8.13.0

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

  # Code Generation
  build_runner: ^2.4.7
  riverpod_generator: ^2.3.9
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  hive_generator: ^2.0.1

# Hinweis: supabase_flutter erfordert Flutter 3.27+ (Dart 3.6+)
# Falls Build fehlschlägt, führe `flutter upgrade` aus

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/data/
    - assets/images/
